{
  "name": "pong-client",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://tingc10.github.io/pong-client/",
  "dependencies": {
    "classnames": "^2.2.6",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-scripts": "3.2.0",
    "socket.io-client": "^2.3.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build",
    "clone-unity-build": "cp -r ./node_modules/Pong/build ./public/unity-build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "Pong": "https://github.com/tingc10/Pong.git",
    "gh-pages": "^2.1.1"
  }
}
